# Example FastAPI application
This demonstrates how a simple API can be built using FastAPI.

To run, use:

```
python3 -m venv venv
. venv/bin/activate
pip install fastapi==0.85.0 uvicorn[standard]==0.18.3
uvicorn app:app
```
