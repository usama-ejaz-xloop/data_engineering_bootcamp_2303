import pandas as pd # type: ignore
from typing import List


def label_encoder(df: pd.DataFrame, columns_to_encode: List[str]) -> pd.DataFrame:

    # Implement a function that modifies only columns of the `df` DataFrame specified in
    # `columns_to_encode` argument. You should follow label encoding approach
    # described in the lecture.

    pass

